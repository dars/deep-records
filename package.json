{
  "name": "deep-records",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-content.mjs",
    "dev": "npm run generate && vite",
    "dev:worker": "npm run generate && wrangler dev",
    "build": "npm run generate && tsc -b && vite build",
    "preview": "vite preview",
    "test": "npm run generate && vitest run",
    "typecheck": "npm run generate && tsc -b",
    "deploy": "npm run build && vitest run && wrangler deploy"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "react-iconly": "^2.2.10"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260717.1",
    "@types/node": "^24.13.2",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "typescript": "~6.0.2",
    "vite": "^8.1.1",
    "vitest": "^4.1.10",
    "wrangler": "^4.111.0"
  }
}
